# Database Configuration
# For local development, use your local PostgreSQL connection string
# For Render, this will be automatically set from your managed PostgreSQL
DATABASE_URL=postgresql://username:password@localhost:5432/attendance_db

# JWT Configuration
# Use a strong, unique secret in production (at least 32 characters)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL for CORS
# In production, set this to your deployed frontend URL
FRONTEND_URL=http://localhost:5173

# Default Admin Bootstrap (used to create initial admin account)
# Change these values or remove them after first setup
DEFAULT_ADMIN_NAME=Admin
DEFAULT_ADMIN_MOBILE=9999999999
DEFAULT_ADMIN_EMPLOYEE_ID=ADMIN001
DEFAULT_ADMIN_PASSWORD=admin123
